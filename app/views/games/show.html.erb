<%= render 'board_layout', game: @game %>

<%= form_tag review_game_url(@game.id), method: 'get' do %>
  <textarea></textarea>
  <button class="ladda-button" data-style="contract" data-color="mint", data-spinner-color="white" id="submitPgn">
    <span class="ladda-label">Submit</span>
  </button>
<% end %>

